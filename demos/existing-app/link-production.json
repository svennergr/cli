{
  "command": "npm run shopify app config link",
  "steps": [
   {
      "type": "output",
      "properties": {
        "content": "To run this command, log in to Shopify Partners."
      }
    },
    {
      "type": "output",
      "properties": {
        "content": "👉 Press any key to open the login page on your browser"
      }
    },
    {
      "type": "output",
      "properties": {
        "content": "✅ Logged in.\n"
      }
    },
    {
      "type": "confirmationPrompt",
      "properties": {
        "message": "Would you like to link to an existing app on Shopify?",
        "confirmationMessage": "Yes, link to an existing app",
        "cancellationMessage": "No, create a new app"
      }
    },
    {
      "type": "selectPrompt",
      "properties": {
        "message": "Which app would you like to link to?",
        "choices": [
          {
            "label": "Printful App",
            "value": "printful"
          },
          {
            "label": "Printful Dev",
            "value": "development"
          },
          {
            "label": "Printful Staging",
            "value": "staging"
          },
          {
            "label": "Printful (Alice)",
            "value": "alice"
          }
        ]
      }
    },
    {
      "type": "textPrompt",
      "properties": {
        "message": "What would you like to name your configuration file?",
        "defaultValue": "shopify.app.printful-app.toml"
      }
    },
    {
      "type": "success",
      "properties": {
        "headline": "`Printful` connected to this codebase.",
        "body": [
          "Using shopify.app.printful-app.toml as the current configuration."
        ],
        "nextSteps": [
          [
            "To see your app, run",
            {
              "command": "shopify app dev"
            }
          ],
          [
            "To push configuration changes from shopify.app.printful-app.toml, run",
            {
              "command": "shopify app config push"
            }
          ],
          [
            "To switch configurations, run",
            {
              "command": "shopify app config use {config_name}"
            }
          ]
        ]
      }
    }
  ]
}
