{
  "command": "npm run shopify app config use printful-staging",
  "steps": [
    {
      "type": "success",
      "properties": {
        "headline": "shopify.app.printful-staging.toml is now the active configuration."
      }
    }
  ]
}
