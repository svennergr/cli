{%- if flavor contains "react" -%}
import React from 'react';
import {
  useExtensionApi,
  render,
  Banner,
  useTranslate,
} from '@shopify/checkout-ui-extensions-react';

render('Admin::Product::Action', () => <App />);

function App() {
  const {extensionPoint} = useExtensionApi();
  const translate = useTranslate();
  return (
    <Banner title="{{ name }}">
      {translate('welcome', {extensionPoint})}
    </Banner>
  );
}

{%- else -%}
import { extend, Banner } from "@shopify/checkout-ui-extensions";

extend("Admin::Product::Action", (root, { extensionPoint, i18n }) => {
  root.appendChild(
    root.createComponent(
      Banner,
      { title: "{{ name }}" },
      i18n.translate('welcome', {extensionPoint})
    )
  );
});
{%- endif -%}
