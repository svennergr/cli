name = "{{name}}"
type = "flow_template"

[template]
# Required -- Can be overridden by translations, but used in Partner dashboard
title = "My Awesome Template"

# Optional -- Can be overridden by translations, but used in Partner dashboard
description = "A Template that helps increase merchant productivity"

# Required -- Must include one or more categories
categories = ["orders", "fraud"]

# Optional
pre_install_note = "You must disable automatic payment capture in the Shopify Admin before using this Template"

# Optional
post_install_note = "Please ensure your have disabled automatic payment capture in the Shopify Admin before using this Template"
